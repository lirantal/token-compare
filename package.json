{
  "name": "token-compare",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host --port 5959",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "gpt-tokenizer": "^2.9.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
