{
  "name": "token-compare",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host --port 5959",
    "build": "vite build",
    "preview": "pnpm run build && wrangler dev",
    "deploy": "pnpm run build && wrangler deploy"
  },
  "dependencies": {
    "gpt-tokenizer": "^2.9.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.31.2",
    "vite": "^6.0.0",
    "wrangler": "^4.81.1"
  }
}
